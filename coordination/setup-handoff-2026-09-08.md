# Repository coordination handoff — September 8, 2026

Historical setup record. The later [folder consolidation](organization.md) supersedes the navigation-only layout and migration next steps below.

Session: Plan Samhain Halloween party (`01a06933-8c9a-72c3-992d-e1570b7d43c5`). Checkout: `/Users/akshet/workspace/halloween-2026`, branch `main`.

## Completed
- Preserved 32 previously uncommitted planning files in checkpoint `43142fb`; this does not approve pending proposals.
- Added root AGENTS.md, sibling task navigation under workstreams/, a session/access guide, handoff template and staged organization plan.
- Reserved apps/hollow-court/ for the user-initiated implementation task; no code or deployment created.
- Inspected available session/project inventory. Local halloween-2026 and ChatGPT Halloween 2026 are distinct; several older sessions have noncanonical working directories.

## Verification and limitations
- Verified local Markdown links across the setup documents and root README; whitespace checks pass.
- Available tools cannot bulk-convert chats or enforce a Work-only project setting. No access settings were changed, sessions messaged or archived, or new tasks created.
- Original planning files retain their paths. Family indexes link to them; physical migration is optional after older-session handoffs.
- Setup commit hash is reported in this session's final response and can be found with `git log`; this file is part of that commit.

## Next actions
1. Start future file-writing sessions in the local repository, directly on `main` in the shared canonical checkout; no worktrees or task branches.
2. If desired, send older sessions the prepared transition message in session-setup.md; reconcile missing context before retiring them.
3. Launch game implementation separately using interactive-game/app/README.md and the latest interactive-story docs.
4. Commit progress as you go on shared `main`; defer large file moves until handoffs are settled.

## Main-only workflow update
User explicitly requested shared `main`, no worktrees, and commits as work progresses. Updated AGENTS.md, the session guide, application workspace guide and this handoff accordingly. Git is primarily a task/decision journal; polished history is unnecessary. Verification: local links and whitespace checks. The resulting commit is reported in the final response.
