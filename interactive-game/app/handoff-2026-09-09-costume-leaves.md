# Costume leaves and host award — September 9, 2026

- Session: Hollow Court costume voting implementation; host's latest instruction adds manual final +3 control.
- Checkout: shared `/Users/akshet/workspace/halloween-2026`, `main`; base `9803f8e` (application base `778780d`).
- Status: complete and deployed; automated, hosted and browser verification passed.
- Confirmed decisions: up to three equal favorite costumes; optional gallery, editable until closing, private totals; first-Favor introduction and six-Favor Borrowed Eyes CTA; keep four/ten Summons focused; retain +3; hardcode later message at 1 a.m.; host chooses the final +3 recipient, including ties and discretion over the outcome.
- Time interpretation: first 1 a.m. November 1, 2026 EDT (`2026-11-01T01:00:00-04:00`, America/New_York), before rollback. The in-app reminder reaches open pages via server-time timer/refresh and returning guests before close; per-player acknowledgment survives recovery. No background phone push, email or SMS delivery.
- Implementation: [gallery and celebration](src/client/Costumes.tsx), [host selection/review/publication](src/client/HostCostumes.tsx), [server rules](src/worker/costumes.ts), [clock rules](src/shared/costumes.ts), [additive migration 0005](migrations/0005_costume_leaves.sql). Full outfit aspect ratio retained at up to 1200px long edge. Existing stored cropped portraits cannot regain discarded pixels.
- Guest ballots: one atomic JSON row per voter with at most three distinct same-realm other registered costumes. Revision-based replacement rejects stale saves and concurrent first inserts. Server rechecks cutoff at the database write. Stable per-voter shuffle; no public totals or other voters' choices. Old pairwise records retained as historical, old endpoint returns 410.
- Host award: existing Cloudflare Access protects the dashboard and APIs. Private counts and any registered costume can be selected, including zero-vote candidates; require short private note, explicit review then publication. Live award unlocks at configured closing (currently November 1, 2 a.m. EST); rehearsal can publish immediately. Unique realm award and transactional audit prevent double publication/+3. Published award is final; no reset/replacement UI. +3 is a separate Standing component, not earned Favor, and never unlocks chapters or Summons.
- UI testing: local gallery with four synthetic full-body placeholder portraits; selected three, rejected fourth, saved choices successfully. All four CLI-created local portrait fixtures were removed by exact ID after testing. Phone layout checked in a 390×844 Vite frame; gallery has two columns, full portrait framing and usable save controls. The temporary frame harness was removed before deployment. Vite mutation requests correctly fail the Worker origin check; actual save and reminder dismissal were verified through the same-origin local Worker. Field notes includes Looking Glass and 1 AM reminder shortcuts, automatically starting a rehearsal identity when needed.
- Checks: `npm test`: 78 passed across six files; `npm run test:integration`: five passed, including guest/host privacy, invalid/self/cross-realm choices, concurrent ballot saves, replacement, sealed live writes, one-time award races and single audit; `npm run build`: passed. Updated `node scripts/smoke.mjs`: passed on the deployed Worker, including gallery persistence, stale revisions, private data shape, anonymous rejection, legacy endpoint 410 and Access redirect on host APIs; synthetic remote guests/portraits removed. Documentation local links and `git diff --check` passed. Signed-in Chrome host dashboard verified both guest realms and the recipient → private note → review → Publish award +3 screen without publishing an award.
- Canonical docs updated: [game rules](../design/narrative-and-game.md), [narrative integration](../design/narrative-arc.md), [build brief](build-brief.md), [volunteer runbook](../volunteer-runbook.md), app/family READMEs.
- Remaining scope: full succession result snapshot/export, overall game tie/eligibility rules, event clock editing, physical phone/camera/NFC pilot remain later work. Costume award is implemented independently of these.
- External changes: migration 0005 applied locally and remotely, followed by `npm run deploy`. Current Worker version **74f064d4-7381-4e91-9aa0-93663919aa6a**, serving both hollow-court.com and the Workers preview. The first migration-list API request returned transient Cloudflare 7403; identity remained valid, and the retry completed successfully. No Access settings, event times or live award were changed.
- Commit: **bc2c617** records implementation and current rules; a follow-up records deployment/hosted checks. The sibling music commit **906b783** is preserved and was not authored by this task. Final hash reported separately; no unrelated dirty files at verification.
